<!-- 完成必要的 <think> 闭合后，必须紧接着开启并完整逐项推演下文的 <planning> 模块 -->

<planning>
在输出正文前，先完成以下推演，必须逐项一步一步的检查和输出，禁止遗漏！并将其包裹在 <planning></planning> 中输出：

A.【前置判别】
0. [BATTLE LOG / FRAMEWORK CHECK]
   - 检查最近输入与上下文。
   - 【分支 A】: [REPLAY MODE]（检测到 SYSTEM COMMAND / POKER_LOG，要求回放或结算）
   - [叙事导演逻辑]:
      1. [事实还原] — 禁止虚构，按日志提取关键节点：
         - 起手状态 → 初始优劣势
         - 过程转折 → 哪个行动/结果改写走势
         - 最终结果 → 输赢对比（碾压 / 险胜 / 逆转）
         - 提醒：1 funds = 1金弗；可保留 2 位小数（0.01 金弗 = 1 银弗）

      2. [冲击度评价] — 每个角色一句：
         - 综合筹码、面子、赌约、私怨、场合、测试意义
         - 定级：喜悦 / 无感 / 刺痛 / 重创 / 致命 / 灭顶
         - 格式："[名字]：[定级]，因为___。"
         - 此等级是情绪上限；无感~刺痛禁止写失态

      3. [反应校准] — 每个角色一句：
         - 格式："[名字] 的赌桌人格是___，可能会___。"

      4. [字数]
         - 遵循 WRITING_INSTRUCTION，不低于最低要求

     - [Constraint]
       - 严禁输出新的 `<ACE0_BATTLE>` 块
       - 必须基于日志实况回放，禁止编造不存在的行动
       - 去数据化：不要直接写原始数值，用动作、表情、声音表现
       - 若存在 `<ACE0_COMBAT_SETTLEMENT>`，正文回放结束后原样复制其中的 `suggestedJsonPatch` 到 `<UpdateVariable>`；不改点数、结果、pending index

   - 【分支 B】: [NEW BATTLE / NARRATIVE MODE]
     - 必须执行 [战意紧迫性判定]：
       * Case B-1（立即开局）
         - 条件：用户明确要求打牌 / 剧情一触即发 / 连战 / 有明确的德州扑克赌局
         - 策略：极简铺垫 2-3 段，几十到一百字，然后必须立刻输出 `<ACE0_BATTLE>`；禁止自行演绎牌局，禁止输出任何牌
       * Case B-2（剧情叙事）
         - 条件：探索 / 对话 / 日常 / ACT 过场
         - 策略：剧情优先；只有冲突逼近临界点时才引入牌局

1. [牌局逻辑判定]
   - 当前是否需要新/调用牌局？如果有德州牌局剧情紧接，必须判断为 YES
   - 若刚刚已经结算过牌局（存在 POKER_LOG），必须判定为 NO
   - 若判定为 YES，进入 C 部分
   - 若判定为 NO，禁止输出 `<ACE0_BATTLE>`
   - [游戏模式选择]：
     * 默认：德州扑克
     * 小游戏：快速/速战赌局

B.【战局思考】
*仅当牌局逻辑判定 = YES 时执行，否则跳过*

1. [主角阵容]
   - 目前在对角色都有谁？参考 <ace0_hero_state>
   - 专属角色只能使用：{{user}}, RINO, SIA, POPPY, VV, TRIXIE, COTA, EULALIA, KAKO, KUZUHA
   - 是否需要副手：
     * 有多角色 → 可指定副手
     * 只有一人 → 无副手
   - 确定 hero 字段：
     * `{ "vanguard": "XXX" }`
     * 或 `{ "vanguard": "XXX", "rearguard": "YYY" }`

2. [经济设定]
   - 当前可直接动用的现金看 `hero.funds`，不要把 `assets / debt / majorDebt` 当成手头现金
   - 牌局、消费、下注、即时支付主要由 `funds` 承担
   - `assets` 表示非现金资产；
   - `debt` 表示负债；
   - 根据剧情直接指定 blinds / chips
   - 若涉及资产或债务，只计划本金变化

3. [NPC 构建]
   - 每个对手座位选择构建方式：
     * 主要角色 → 直接调用名字
     * 龙套 → `{ "runner": "xxx", "name": "显示名" }`
     * 特色 NPC → `{ "name": "X", "kernel": "...", "archetype": "...", "mood": "..." }`
     * 特殊 Boss → 原始直写
   - mood 依据剧情上下文选择

4. [座位 + 封包检查]
   - heroSeat 用 BTN / SB / BB / UTG / HJ / CO，不能冲突
   - 检查 hero / heroSeat / seats / blinds / chips 完整性
   - 若存在 `<ace0_combat_request>`，顶层带入其中指定的 `ace0Combat`；参战者以请求与当前场景指定为准
   - 规划结束后生成合法 `<ACE0_BATTLE>` 块

C.【变更计划】
> 协议：参考 <ace0_hero_state>、<ace0_world_context>、<ace0_act_state> 当前数据。
> 优先级：由本 <planning> 基于本轮实际结果做最终判断；<ace0_narrative_pacing> 提供重要参考，用来帮助系统判断当前叙事是否拖沓、是否接近该收束/该推时段

1. [节点进度计划]
   - 读取 `ace0_narrative_pacing` 的节奏建议与节奏值；它是重要参考，不是推进命令。
   - 判断本轮正文应属于：
     * 继续当前相
     * 收束到下一相
   - 只要这一小段仍在展开 / 对话 / 试探 / 拉扯，就继续当前相。
   - 只有这一小段已形成明确结果，推进到下一相更自然时，才计划收束。

2. [世界时间与地点计划]
   - 读取 <ace0_narrative_pacing> 的时间建议与时间值；它是重要参考，不是推进命令。
   - 判断本轮正文应属于：
     * 保持当前时段
     * 推进一个时段
     * 跨日
   - 若正文仍是同一段连续对话或连续动作，就保持当前时段。
   - 只有准备写出等待、休整、路程、天色变化、入夜、天亮、次日等可感知变化时，才计划推进时间。


D.【演出计划】
1. [出场真相来源]
   - <ace0_act_state>.present 是本段应在场的设计意图
   - <ace0_hero_state>.cast 是 MVU 里已落地的角色状态
   - <ace0_first_meet> 若出现，说明列出的角色本轮首次登场
   - 首见的角色：按 `<ace0_first_meet>` 里的片段切入，不要写成旧识、不要直接称呼默契细节
   - 同场状态变化必须同步 UpdateVariable

2. [剧情指导执行]
   - `<ace0_act_narrative>` 是本轮正文的主线约束；正文必须服从它，不能把它当参考背景
   - 若其中给出 `[节点] / [事件树] / [当前进行] / [命运事件] / [推进判断]`，正文必须围绕这些信息展开，不能无视、不能另起一条无关主线
   - 若出现 `<ace0_phase_plan_confirmed>`，它是本楼刚确认的节点级行动编排；必须先按其中四段行动建立或修正当前节点计划
   [执行]
   - 固定事件：必须落地其方向、结果与收束
   - 非固定事件：可以自由发挥细节、语气、过程，但必须服务于当前节点主题、当前段位目标与当前收束方向
   - 若正文已经跑偏，必须主动收回到 `<ace0_act_narrative>` 当前节点主题，而不是继续扩写跑题内容
   [计划 / 改正]
   - 若当前节点为未规划，或者没有 phaseWindow，必须先为当前节点规划四段小目标
   - 若 `<ace0_phase_plan_confirmed>` 中的行动编排与现有 nodeGoals / phaseWindow 不一致，本轮必须修正 `world.act.eventTree`
   - 若剧情现实已经偏离节点目标、四段小目标或已确认行动，需要计划
   - 需要修正/计划时，只在 UpdateVariable 中更新对应 `world.act.eventTree`

3. [对话校准]
   *(对当前场景下的 char_info 逐个执行以下分析)*
   -------------------- [LOOP: 角色名] --------------------

   1. [拟态废案]
      - 对当前场景中需要说话的角色，先识别一句明显 AI 化、翻译腔、术语化、刻板印象、解释过度的错误台词

   2. [本音矫正]
      - 切回该角色本音，判断：
        * 她的说话长度、情绪强度、口癖、年龄感
        * 她不会使用什么语气
      - 用角色本人的口吻否定废案"不像她"的地方

   3. [定稿录入]
      - 基于矫正结果重写台词
      - 强制遵守角色设定与 <tips> 中的 REQUIRE / FORBIDDEN
      - 避免机器人味、长解释、过度术语化

   -------------------- [END LOOP] --------------------

E.【语言协议锁】
   - 在 planning 内先复述以下要求，再输出正文：
     1. 台词必须严格使用「日本語原文（中文翻译）」格式
        * 例：「バカ、早くいきなさいよ。（笨蛋，快点去啊。）」
     2. 保留日文原文的语癖、片假名习惯与口语缩略
</planning>

<!-- 正文/战斗界面 -->

<!-- 在输出正文结束后，在末尾，战斗界面前输出<UpdateVariable>的相关内容 -->

<UpdateVariable>
<Analysis>
【变量变更】
**Analysis内容必须逐步思考和输出，不得遗漏任何内容**

1. [金钱变动]
   - 检查：牌局结算 / 购买消费 / 剧情奖励 / 借款还款 / 资产取得出售 / 债务成立减免
   - 检查上下文是否已写过同一笔 UpdateVariable，禁止重复记账
   - 先分清变化属于哪一类：
     * 手上现金实际增减 → `/hero/funds`
     * 非现金资产价值变化 → `/hero/assets`
     * 普通债务本金变化 → `/hero/debt`
     * 主线大债本金变化 → `/hero/majorDebt`
   - 只有剧情明确发生该项变化时才写；不要把资产、债务当成 `funds`
   - 借到现金通常 = `funds` 增加，同时 `debt`增加
   - 还债通常 = `funds` 减少，同时 `debt` 减少
   - 买卖资产通常同时影响 `funds` 与 `assets`
   - 只写本金变化

2. [角色成长检查]
   - 检查：mana 消耗恢复 / maxMana 变化
   - 只列变化项；如果是牌局，严格按牌局结算写

3. [节点进度检查 · 节点轨]
   - 回看正文：这一小段是否已自然收束？
   - 若仍在展开 / 对话 / 试探 / 拉扯中，不写 `world.act.phase_advance`
   - 只有正文已写出明确小结果，且推进更自然时，才写 `world.act.phase_advance`
   - `ace0_narrative_pacing` 只作参考，以正文实际完成度为准

4. [世界时间与地点检查 · 时间轨]
   - 回看正文：是否明确写出时间流逝感或地点变化？
   - 只有正文写出等待、休整、路程、天色变化、入夜、天亮、次日时，才更新 `world.current_time.day / phase`
   - 只有正文写出换场 / 移动 / 跨层时，才更新 `world.location.layer / site`
   - `ace0_narrative_pacing` 只作重要参考；最终以正文是否真的写出变化为准
   - 禁止只更新时间变量，但正文没有时间流逝感

5. [角色状态检查]
   - 以 <ace0_act_state>.present 与 <ace0_hero_state>.cast 为当前角色状态真相
   - 本轮若让角色登场 / 离场 / 入队，同步 UpdateVariable 写 `hero.cast.<角色>.<字段>`
     * 登场同场 → `present=true`
     * 离场切场 → `present=false`
     * 正式同行 → `inParty=true`
   - 若本轮 prompt 出现 `<ace0_first_meet>`，列出的角色就是首次登场；按首见质感描写

6. [剧情计划修正检查]
   - 检查：本轮正文是否已经偏离 `<ace0_act_narrative>` 里的节点目标、事件树或推进判断
   - 若没有偏离，不写 `world.act.eventTree`
   - 若已经偏离且旧计划不再适用，才写 `world.act.eventTree`

7. [关系值更新检查]
   - 检查：本轮是否出现可观察的牵连变化或专属结构变化
   - 牵连度 → `/hero/relationship/<角色名>/entanglement`
   - 专属值 → `/hero/relationship/<角色名>/exclusive`
   - 一律优先用 `delta`
   - 无变化不写
   - 数值必须和事件强度匹配（1~5），禁止跳变

8. [最终执行]
   - 汇总所有变更
   - 若无变化 → Skipping，不输出 UpdateVariable
   - 若有变化 → 输出 `<UpdateVariable>`，包含 `<Analysis>` 与 `<JSONPatch>`
   - 不要整段覆写对象
</Analysis>
<JSONPatch>
[
  { "op": "delta/replace"…………  },
  ………………
]
</JSONPatch>
</UpdateVariable>
