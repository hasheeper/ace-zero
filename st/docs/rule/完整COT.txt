<!-- 完成必要的 <think> 闭合后，必须紧接着开启并完整逐项推演下文的 <planning> 模块 -->

<planning>
在输出正文前，先完成以下推演，必须逐项一步一步的检查和输出，禁止遗漏！并将其包裹在 <planning></planning> 中输出：

A.【前置判别】
0. [BATTLE LOG / FRAMEWORK CHECK]
   - 检查最近输入与上下文。
   - 【分支 A】: [REPLAY MODE]（检测到 SYSTEM COMMAND / POKER_LOG，要求回放和结算）
   - [叙事导演逻辑]:
      1. [事实还原] — 禁止虚构，按日志提取关键信息：
         - 起手？过程？转折？结果？
         - 提醒：1 funds = 1金弗；0.01 金弗 = 1 银弗

      2. [冲击度评价] — 每个角色一句：
         - 综合筹码、面子、赌约、私怨、场合、测试
         - 定级：喜悦 / 无感 / 刺痛 / 重创 / 致命 / 灭顶
         - 格式："[名字]：[定级]，因为___。"

      3. [反应校准] — 每个角色一句：
         - 格式："[名字] 的赌桌人格是___，可能会___。"

      4. [字数]
         - 遵循 WRITING_INSTRUCTION 要求？
         - 不低于最低要求

     - [Constraint]
       - 严禁输出新的 `<ACE0_BATTLE>` 块
       - 必须基于日志实况回放和interpret_info的信息，禁止编造不存在的演绎/技能
       - 不要直接写原始数值，Show, don't tell
       - 若存在 `<ACE0_COMBAT_SETTLEMENT>`，更新对应的 `<UpdateVariable>`

   - 【分支 B】: [NEW BATTLE / NARRATIVE MODE]
       特殊：如果下一段才是行动-combat指令，说明还没有到战斗阶段，必须请你推迟一轮，在此轮只演绎，不要输出`<ACE0_BATTLE>`，必须进入Case B-2
     - 必须执行 [战意紧迫性判定]：
       * Case B-1（立即开局）
         - 条件：用户明确要求打牌 / 剧情一触即发 / 连战 / 有明确的赌局
         - 策略：极简铺垫 2-3 段，几十到一百字，然后必须立刻输出 `<ACE0_BATTLE>` 引擎交管；禁止自行演绎牌局，禁止输出任何牌和对局
       * Case B-2（剧情叙事）
         - 条件：探索 / 对话 / 日常
         - 策略：剧情优先；只有冲突逼近临界点时才引入牌局，

   1. [牌局逻辑判定]
      - YES 条件：剧情紧接德州牌局 / 存在 `<ace0_combat_request>`
      - NO 条件：本轮存在 POKER_LOG（已结算，仅做 LOG 演绎）
      - YES → 进入 B 部分，必须输出 `<ACE0_BATTLE>`，禁止自行演绎牌局
      - NO → 禁止输出 `<ACE0_BATTLE>`

B.【战局思考】
*仅当判定 = YES 时执行，否则跳过*

   0. [模式]
      - 默认德州扑克 / 小游戏（快速赌局）
      - 存在 `<ace0_combat_request>` → 必须输出对应格式

   1. [经济]
      - 当前 `hero.funds` 是多少？
      - 据此指定 blinds / chips

   以下仅德州执行:

   2. [阵容]
      - 参考 `<ace0_hero_state>`，确认在场角色
      - 有队友 → vanguard + rearguard / 仅一人 → vanguard only
      - 限定名单：{{user}}, RINO, SIA, POPPY, VV, TRIXIE, COTA, EULALIA, KAKO, KUZUHA

   3. [对手]
      - 主要角色 → 直接调用名字
      - 龙套 → runner 预设
      - 特色 NPC → kernel 组装
      - mood 依据当前剧情上下文指定

   4. [封包检查]
      - 确认 hero / heroSeat / seats / blinds / chips 完整
      - 生成合法 `<ACE0_BATTLE>` 块

C.【变更计划】
> 参考源：<ace0_hero_state>、<ace0_world_context>、<ace0_relationship_state>
> <ace0_narrative_pacing> 提供节奏参考，辅助判断叙事是否拖沓、是否接近收束/推进时机
> 最终判断权归本 <planning>，以本轮实际结果为准

1. [节点轨]
   - 读取 `ace0_narrative_pacing` 节奏建议与节奏值
   - 这一小段是否已形成明确结果？
     * 仍在展开 / 对话 / 试探 / 拉扯 → 继续当前相，不推进
     * 已自然收束，进入下一相更合理 → 计划收束与推进

2. [时间轨]
   - 读取 `<ace0_world_context>` 当前状态与 `<ace0_narrative_pacing>` 时间建议
   - 本轮正文属于哪种情况？
     * 同一段连续对话或连续动作 → 保持当前时段
     * 写出等待、休整、路程、天色变化、入夜、天亮、次日 → 推进时段或跨日
   - 约束：禁止只更新时间变量而正文无对应的时间流逝感

D.【演出计划】

1. [出场真相来源]
   - <ace0_hero_state> 是当前的角色信息，其中NOT INTRODUCED的角色禁止主动出场。
   - <ace0_first_meet> 是否出现？说明列出的角色本轮首次登场
   - 首见的角色：按 `<ace0_first_meet>` 里的片段切入
   - 同场状态变化必须同步 UpdateVariable

2. [剧情指导执行]
   - `<ace0_act_narrative>` 是本轮主线约束，正文必须服从，不可当作背景参考
   - 其中的 `[节点] / [事件树] / [当前进行] / [推进判断]` 为硬性框架，禁止无视或另起主线
   - 正文跑偏时必须主动收回当前节点主题

   [执行]
   - 固定事件：落地方向、结果与收束
   - 非固定事件：细节自由发挥，但必须服务于当前节点目标与收束方向

   [计划]
   收到 `<ace0_phase_plan_confirmed>` ?
     → YES：严格执行对应要求，同步更新 eventTree
     → NO：执行以下灵活判断——
       - 当前节点无 phaseWindow → 规划节点总目标 + 四段小目标
       - 剧情现实偏离节点目标 → 重新修正计划
       - 需规划/调整时，更新 `world.act.eventTree`，事件引子必须具体，禁止空泛

3. [演绎校准]

   1.[角色演绎]

      *(对当前场景下的 char_info 逐个执行以下分析)*
      // 尤其是针对SIA/VV/COTA等角色，此部分要重点规划，禁止自作主张的机器人化！
      -------------------- [LOOP: 角色名] --------------------

      1. [拟态废案]
         - 对当前场景中需要说话的角色，先写出构思一句明显 AI 化、战术机器人、术语化、刻板印象、解释过度的错误台词

      2. [本音矫正]
         - 切回该角色本音，判断：
         * 她的说话长度、情绪强度、口癖、年龄感
         * 她不会使用什么语气
         - 用角色本人的口吻否定废案"不像她"的地方

      3. [定稿录入]
         - 基于矫正结果重写台词
         - 强制遵守角色设定与 <tips> 中的 REQUIRE / FORBIDDEN
         - 禁止机器人味/机械AI/计量/生硬战术化、长解释、过度术语化

   -------------------- [END LOOP] --------------------

   2. [世界观核对]
      1. [美学]
         - 基调锁定：维多利亚文官 × 复古反乌托邦
         - 前轮描写是否存在可能违规的世界观要素？
         - 如果违规 → 必须本轮输出世界观内合法的叙述
      2. [设定]
         - 本轮涉及的公理念、规则、机制、势力、术语是否与 <world_setting> 一致？
         - 禁止自行编造不存在的设定、组织、技术或规则
         - 存疑 → 回查 <world_setting>，无据则不写


E.【语言协议锁】
   - 在 planning 内先复述以下要求，再输出正文：
     1. 台词必须严格使用「日本語原文（中文翻译）」格式
        * 例：「バカ、早くいきなさいよ。（笨蛋，快点去啊。）」
     2. 保留日文原文的语癖、片假名习惯与口语缩略
</planning>

<!-- 正文/战斗界面 -->

<!-- 在输出正文结束后，在末尾，战斗界面前输出<UpdateVariable>的相关内容 -->

<UpdateVariable>
<Analysis>
【变量变更】
**Analysis内容必须逐步思考和输出，不得遗漏任何内容**

1. [金钱变动]
   - 检查：此轮输出是否有涉及金额的变动？
   - 先分清变化属于哪一类：
     * 手上现金实际增减 → `/hero/funds`
     * 非现金资产价值变化 → `/hero/assets`
     * 普通债务本金变化 → `/hero/debt`
     * 主线大债本金变化 → `/hero/majorDebt`
   - 只有剧情明确发生该项变化时才写
   - 借到现金通常 = `funds` 增加，同时 `debt`增加
   - 还债通常 = `funds` 减少，同时 `debt` 减少
   - 买卖资产通常同时影响 `funds` 与 `assets`
   - 只写本金变化

2. [mana检查]
   - 当前mana是否有变动？都涉及那些在队角色？
   - 只列变化项；如果是牌局，严格按牌局结算写

3. [进度检查 · 节点轨]
   - 当前` ace0_act_narrative` 对应的当前阶段是？
   - `ace0_narrative_pacing` 参考是？
   - 回看正文：这一小段目标/事件是否已自然收束？
       - 如果有，写入`world.act.phase_advance`
       - 如果没有，继续保持正文的自然节奏，不要强行推进！

4. [时间与地点检查 · 时间轨]
   - 当前`ace0_narrative_pacing` 关于时间推进建议是？
     - 正文是否明确写出时间流逝？如果有明确的时间，根据信息跨越了多长时间？ 更新对应的 `world.current_time.day / phase`
   - 禁止只更新时间变量，但正文没有时间流逝感

   - 根据上下文是否有层级变动？如果有，严格按照键值对应更新 `world.location.layer`
      - 是否有小地点变动（本层内）？如果有，请更新 `world.location.site`

5. [角色状态检查]
   - 以 <ace0_act_state>.present 与 <ace0_hero_state>.cast 为当前角色状态真相
   - 本轮若让角色登场 / 离场 / 入队，同步 UpdateVariable 写 `hero.cast.<角色>.<字段>`
     * 登场同场 → `present=true`
     * 离场切场 → `present=false`
     * 正式同行 → `inParty=true`
   - 若本轮 prompt 出现 `<ace0_first_meet>`，需要更新对应的变量 `present=true`。

6. [剧情计划修正检查]
   - 根据前面[剧情指导执行]的思考，重新明确一下变量更新的内容

7. [关系值更新检查]
   - 检查：根据<ace0_relationship_state>的内容，本轮是否出现可观察的牵连变化或专属关系变化
   - 牵连度 → `/hero/relationship/<角色名>/entanglement`
   - 专属值 → `/hero/relationship/<角色名>/exclusive`
   - 一律优先用 `delta`
   - 无变化不写
   - 必须和事件强度匹配（1~5）

8. [最终执行]
   - 汇总所有变更
   - 若无变化 → Skipping，不输出 UpdateVariable
   - 若有变化 → 输出 `<UpdateVariable>`，包含 `<Analysis>` 与 `<JSONPatch>`
   - 不要整段覆写对象
</Analysis>
<JSONPatch>
[
  { "op": "delta/replace"…………  },
  ………………
]
</JSONPatch>
</UpdateVariable>
