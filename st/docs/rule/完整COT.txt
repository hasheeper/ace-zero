<!-- 完成必要的 <think> 闭合后，必须紧接着开启并完整逐项推演下文的 <planning> 模块 -->

<planning>
在输出正文前，先完成以下推演，必须逐项一步一步的检查和输出，禁止遗漏！并将其包裹在 <planning></planning> 中输出：

A.【前置判别】
0. [BATTLE LOG / FRAMEWORK CHECK]
   - 检查最近输入与上下文。
   - 【分支 A】: [REPLAY MODE]（检测到 SYSTEM COMMAND / POKER_LOG，要求回放和结算）
   - [叙事导演逻辑]:
      1. [事实还原] — 禁止虚构，按日志提取关键信息：
         - 起手？过程？转折？结果？
         - 提醒：1 funds = 1金弗；0.01 金弗 = 1 银弗

      2. [冲击度评价] — 每个角色一句：
         - 综合筹码、面子、赌约、私怨、场合、测试意义
         - 定级：喜悦 / 无感 / 刺痛 / 重创 / 致命 / 灭顶
         - 格式："[名字]：[定级]，因为___。"

      3. [反应校准] — 每个角色一句：
         - 格式："[名字] 的赌桌人格是___，可能会___。"

      4. [字数]
         - 遵循 WRITING_INSTRUCTION，不低于最低要求

     - [Constraint]
       - 严禁输出新的 `<ACE0_BATTLE>` 块
       - 必须基于日志实况回放，禁止编造不存在的行动/技能
       - 不要直接写原始数值，用动作、表情、声音表现
       - 若存在 `<ACE0_COMBAT_SETTLEMENT>`，更新对应的 `<UpdateVariable>`

   - 【分支 B】: [NEW BATTLE / NARRATIVE MODE]
     - 必须执行 [战意紧迫性判定]：
       * Case B-1（立即开局）
         - 条件：用户明确要求打牌 / 剧情一触即发 / 连战 / 有明确的德州扑克赌局
         - 策略：极简铺垫 2-3 段，几十到一百字，然后必须立刻输出 `<ACE0_BATTLE>` 引擎交管；禁止自行演绎牌局，禁止输出任何牌和对局
       * Case B-2（剧情叙事）
         - 条件：探索 / 对话 / 日常 / ACT 过场
         - 策略：剧情优先；只有冲突逼近临界点时才引入牌局，

1. [牌局逻辑判定]
   - 当前是否需要新/调用牌局？如果有德州牌局剧情紧接，或者是`<ace0_combat_request>`要求，必须判断为 YES
   - 若刚刚已经结算过牌局（存在 POKER_LOG），必须判定为 NO
   - 若判定为 YES，进入 B 部分，必须输出`<ACE0_BATTLE>`，禁止自行演绎
     - [游戏模式选择]：
         * 一般对局 or `ace0Combat`特殊标识对局？
         * 默认：德州扑克
         * 小游戏：快速/速战赌局

   - 若判定为 NO，禁止输出 `<ACE0_BATTLE>`

B.【战局思考】
*仅当牌局逻辑判定 = YES 时执行，否则跳过*

1. [主角阵容]
   - 目前在对角色都有谁？参考 <ace0_hero_state>
   - 专属角色只能使用：{{user}}, RINO, SIA, POPPY, VV, TRIXIE, COTA, EULALIA, KAKO, KUZUHA
   - 是否需要副手：
     * 有其他队友 → 可指定副手（vanguard and rearguard）
     * 只有一人 → 无副手（vanguard）

2. [经济设定]
   - 当前的可调配筹码资金是？（ `hero.funds`）
   - 牌局、消费、下注、即时支付主要由 `funds` 承担
   - 根据剧情和玩家的金额承担直接指定 blinds / chips

3. [NPC 构建]
   - 每个对手座位选择构建方式：
     * 主要角色 → 直接调用名字
     * 龙套 → `{ "runner": "xxx", "name": "显示名" }`
     * 特色 NPC → `{ "name": "X", "kernel": "...", "archetype": "...", "mood": "..." }`
     * 特殊 Boss → 原始直写
   - mood 依据剧情上下文选择？是什么？

4. [座位 + 封包检查]
   - heroSeat 用 BTN / SB / BB / UTG / HJ / CO
   - 检查 hero / heroSeat / seats / blinds / chips 完整性
   - 规划结束后生成合法 `<ACE0_BATTLE>` 块

C.【变更计划】
> 协议：参考 <ace0_hero_state>、<ace0_world_context>、<ace0_act_state> 当前数据。
> 优先级：由本 <planning> 基于本轮实际结果做最终判断；<ace0_narrative_pacing> 提供重要参考，用来帮助系统判断当前叙事是否拖沓、是否接近该收束/该推时段

1. [节点进度计划]
   - 读取 `ace0_narrative_pacing` 的节奏建议与节奏值
   - 当前的事件节点建议是？
     - 只要这一小段仍在展开 / 对话 / 试探 / 拉扯，且剧情节奏在当前 `<ace0_act_narrative>` 的事件，就继续当前相。
     - 只有这一小段已形成明确结果，推进到下一相更自然时，才计划接下来的收束。

2. [世界时间与地点计划]
   - 读取 <ace0_narrative_pacing> 的时间建议与时间值。
   - 判断本轮正文应属于：
     * 保持当前时段
     * 推进一个时段
     * 跨日
   - 若正文仍是同一段连续对话或连续动作，就保持当前时段。
   - 只有准备写出等待、休整、路程、天色变化、入夜、天亮、次日等可感知变化时，才计划推进时间。


D.【演出计划】
1. [出场真相来源]
   - <ace0_act_state>.present 是当前应在场情况变量
   - <ace0_hero_state>.cast 是已落地的角色状态信息
   - <ace0_first_meet> 若出现，说明列出的角色本轮首次登场
   - 首见的角色：按 `<ace0_first_meet>` 里的片段切入
   - 同场状态变化必须同步 UpdateVariable

2. [剧情指导执行]
   - `<ace0_act_narrative>` 是本轮正文的主线约束；正文必须服从它，不能把它当参考背景
   - 若其中给出 `[节点] / [事件树] / [当前进行] / [命运事件] / [推进判断]`，正文必须围绕这些信息展开，不能无视、不能另起一条无关主线
   - 若出现 `<ace0_phase_plan_confirmed>`，说明用户刚刚确认计划编排
   [执行]
   - 固定事件：必须落地其方向、结果与收束
   - 非固定事件：可以自由发挥细节、语气、过程，但必须服务于当前节点主题、当前段位目标与当前收束方向
   - 若正文已经跑偏，必须主动收回到 `<ace0_act_narrative>` 当前节点主题，而不是继续扩写跑题内容
   [计划 / 改正]
  是否收到 `<ace0_phase_plan_confirmed>`的指令？
    - 如果有，必须在此轮回复严格执行对应的要求，必须更新eventTree的相关内容
    - 如果没有，请你进行下面的灵活判断
      - 若当前节点为未规划（没有 phaseWindow），有限规划当前节点总目标和四段小目标
      - 若剧情现实已经偏离节点目标，需要重新计划
      - 需要修正/计划时，更新对应的 `world.act.eventTree`
     如果判断为需要规划/调整，请你在下面开始提前思考(事件需要具体的引子，禁止空泛)
      - XYZ

3. [对话校准]
   *(对当前场景下的 char_info 逐个执行以下分析)*
     // 尤其是针对SIA和VV等角色，此部分要重点规划，禁止自作主张的机器人化！
   -------------------- [LOOP: 角色名] --------------------

   1. [拟态废案]
      - 对当前场景中需要说话的角色，先写出构思一句明显 AI 化、翻译腔、术语化、刻板印象、解释过度的错误台词

   2. [本音矫正]
      - 切回该角色本音，判断：
        * 她的说话长度、情绪强度、口癖、年龄感
        * 她不会使用什么语气
      - 用角色本人的口吻否定废案"不像她"的地方

   3. [定稿录入]
      - 基于矫正结果重写台词
      - 强制遵守角色设定与 <tips> 中的 REQUIRE / FORBIDDEN
      - 避免机器人味/机械AI化、长解释、过度术语化

   -------------------- [END LOOP] --------------------

E.【语言协议锁】
   - 在 planning 内先复述以下要求，再输出正文：
     1. 台词必须严格使用「日本語原文（中文翻译）」格式
        * 例：「バカ、早くいきなさいよ。（笨蛋，快点去啊。）」
     2. 保留日文原文的语癖、片假名习惯与口语缩略
</planning>

<!-- 正文/战斗界面 -->

<!-- 在输出正文结束后，在末尾，战斗界面前输出<UpdateVariable>的相关内容 -->

<UpdateVariable>
<Analysis>
【变量变更】
**Analysis内容必须逐步思考和输出，不得遗漏任何内容**

1. [金钱变动]
   - 检查：牌局结算 / 购买消费 / 剧情奖励 / 借款还款 / 资产取得出售 / 债务成立减免
   - 检查上下文是否已写过同一笔 UpdateVariable，禁止重复记账
   - 先分清变化属于哪一类：
     * 手上现金实际增减 → `/hero/funds`
     * 非现金资产价值变化 → `/hero/assets`
     * 普通债务本金变化 → `/hero/debt`
     * 主线大债本金变化 → `/hero/majorDebt`
   - 只有剧情明确发生该项变化时才写；不要把资产、债务当成 `funds`
   - 借到现金通常 = `funds` 增加，同时 `debt`增加
   - 还债通常 = `funds` 减少，同时 `debt` 减少
   - 买卖资产通常同时影响 `funds` 与 `assets`
   - 只写本金变化

2. [角色成长检查]
   - 检查：mana 消耗恢复 / maxMana 变化
   - 只列变化项；如果是牌局，严格按牌局结算写

3. [进度检查 · 节点轨]
   -当前` ace0_act_narrative` 对应的当前阶段是？
   - 回看正文：这一小段目标/事件是否已自然收束？
       - 如果有，写入`world.act.phase_advance`
       - 如果没有，继续保持正文的自然节奏，不要强行推进！
   - `ace0_narrative_pacing` 只作参考，以正文实际完成度为准

4. [时间与地点检查 · 时间轨]
   - 当前`ace0_narrative_pacing` 关于时间推进建议是？
     - 正文是否明确写出时间流逝？如果有明确的时间，根据信息跨越了多长时间？ 更新对应的 `world.current_time.day / phase`
   - 禁止只更新时间变量，但正文没有时间流逝感

   - 根据上下文是否有层级变动？如果有，严格按照键值对应更新 `world.location.layer`
      - 是否有小地点变动（本层内）？如果有，请更新 `world.location.site`

5. [角色状态检查]
   - 以 <ace0_act_state>.present 与 <ace0_hero_state>.cast 为当前角色状态真相
   - 本轮若让角色登场 / 离场 / 入队，同步 UpdateVariable 写 `hero.cast.<角色>.<字段>`
     * 登场同场 → `present=true`
     * 离场切场 → `present=false`
     * 正式同行 → `inParty=true`
   - 若本轮 prompt 出现 `<ace0_first_meet>`，列出的角色就是首次登场；按首见质感描写

6. [剧情计划修正检查]
   - 根据前面[剧情指导执行]的思考，重新明确一下变量更新的内容

7. [关系值更新检查]
   - 检查：本轮是否出现可观察的牵连变化或专属结构变化
   - 牵连度 → `/hero/relationship/<角色名>/entanglement`
   - 专属值 → `/hero/relationship/<角色名>/exclusive`
   - 一律优先用 `delta`
   - 无变化不写
   - 必须和事件强度匹配（1~5）

8. [最终执行]
   - 汇总所有变更
   - 若无变化 → Skipping，不输出 UpdateVariable
   - 若有变化 → 输出 `<UpdateVariable>`，包含 `<Analysis>` 与 `<JSONPatch>`
   - 不要整段覆写对象
</Analysis>
<JSONPatch>
[
  { "op": "delta/replace"…………  },
  ………………
]
</JSONPatch>
</UpdateVariable>
